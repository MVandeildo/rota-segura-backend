const {Schema, model} = require("")

const usuarioSchema = new Schema({
    email: {
        type: String,
        required: true,
        unique: true,
    },
    password:{
        type: String,
        required: true,
    },
    verified:{
        type: boolean,
        default: false,
    },
    refreshToken:{
        type: String,
    },
});

module.exports = model("User", userSchema);
